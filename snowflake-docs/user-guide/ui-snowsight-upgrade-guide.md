---
title: "About the Snowsight upgrade"
url: "https://docs.snowflake.com/en/user-guide/ui-snowsight-upgrade-guide"
---

# About the Snowsight upgrade

Important

With the general enablement of the [2025\_06 behavior change bundle](../release-notes/bcr-bundles/2025_06_bundle),
the Snowsight upgrade is complete. All customers are now in Stage 3 (Snowsight Only)
where Snowsight is the only interface available and Classic Console is no longer accessible.

As a Snowflake user, your web interface access was in one of the following stages during the Snowsight upgrade process:

|  |  |  |  |  |
| --- | --- | --- | --- | --- |
| [Stage 1: Users choose default: Users can choose their default web interface](#label-snowsight-user-default). | [arrow pointing right](../_images/sf-arrow.png) | [Stage 2: Snowsight is default: All users in the account have Snowsight as the default web interface](#label-snowsight-account-default). | [arrow pointing right](../_images/sf-arrow.png) | [Stage 3: Snowsight only: Snowsight is the only interface available](#label-snowsight-organization-only) |

See moreShow less

Expand

## Upgrade stages

[Stage 1: Users choose default: Users can choose their default web interface](#label-snowsight-user-default).

[Stage 2: Snowsight is default: All users in the account have Snowsight as the default web interface](#label-snowsight-account-default).

[Stage 3: Snowsight only: Snowsight is the only interface available](#label-snowsight-organization-only).

### Stage 1: Users choose default: Users can choose their default web interface

If Snowsight is not the default web interface for all users in your account, you can choose to set Snowsight as the
default for your user profile.

### Stage 2: Snowsight is default: All users in the account have Snowsight as the default web interface

When Snowsight is set as the default interface for an account, individual users can no longer set a default web interface on
their user profile.

### Stage 3: Snowsight only: Snowsight is the only interface available

Snowsight is the only interface available. Users cannot access Classic Console.

## Share feedback about Snowsight

If you have concerns about Snowsight replacing your existing workflow with the Classic Console,
contact your Snowflake account representative.
